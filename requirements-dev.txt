# ---------------------------------------------------------------- #
# CartAPI - development / test dependencies
# Install with:  pip install -r requirements-dev.txt
# Combines runtime + dev (so CI can use a single install line).
# ---------------------------------------------------------------- #

-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
ruff>=0.4.0
bandit[toml]>=1.7.0
mypy>=1.10.0
pytest-mock>=3.14
hypothesis>=6.100
factory-boy>=3.3
pytest-benchmark>=4.0
